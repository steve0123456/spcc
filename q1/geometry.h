#ifndef GEOMETRY
#define GEOMETRY

#define pi 3.14

#define area_circle(r) (pi * (r) * (r))

#define area_square(l) ((l) * (l))

#define area_rect(l,b) ((l) * (b))

#define area_tri(l,h) (0.5 * (l) * (h))

#endif